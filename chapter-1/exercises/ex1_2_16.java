// The error in the code inputted is that parenthesis were not used to set precedence that resembled the actual physics code.
// Correction to the code could be this:
// double force = (G * mass1 * mass2)/(r * r);
